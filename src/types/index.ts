export type Hello = {
    name: string
}

export type CardContext = {
    layout: number,
    height: number,
    width: number,
    font: string,
    fontSize: number,
    textAlign: string,
    border: boolean,
    borderStyle: string,
    borderWidth: number,
    borderRadius: number,
    borderColor: string,
}